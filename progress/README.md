# Progress

This directory stores UPSK/Codex learning artifacts, evidence, notes, and recovery metadata.

The original untracked `progress/` contents were not recoverable from Git history because the repository had no commits before the cleanup. Use `progress/evidence/` for module proof and keep recovery notes in this folder so future rebuilds can distinguish restored state from reconstructed state.
